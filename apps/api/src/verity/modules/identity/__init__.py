"""Identity module — authentication, sessions, and account lifecycle.

Bounded module per FolderStructure.md §2. Only `domain/` exists as of Task
2.1 Part B (the `User` model). `application/`, `adapters/`, `contracts/`,
and `tests/` are added in later tasks alongside OAuth, JWT routes, and
refresh-session persistence.
"""